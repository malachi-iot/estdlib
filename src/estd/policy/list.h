#pragma once

// We'll be migrating list_node traits here, since it's overgrown what traits
// really should be and also sometimes manages state ala impl::dynamic_array

namespace estd { namespace internal {

}}
