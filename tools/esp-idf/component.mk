# experimental!  not sure if pathing is gonna be convenient using this
ESDTLIB_DIR := ../..
COMPONENT_ADD_INCLUDEDIRS += ${ESTDLIB_DIR}/src/estdlib
COMPONENT_SRCDIRS += ${ESTDLIB_DIR}/src/estdlib/estd/internal

